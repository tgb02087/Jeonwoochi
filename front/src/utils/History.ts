/**
 * @description
 * 뒤로가기 감지를 위한 라이브러리 모듈
 * react-router v6에는 뒤로가기 감지 라이브러리 없음 (슬프네)
 *
 * @author bell
 */

import { createBrowserHistory } from 'history';

export const history = createBrowserHistory();
