server:
  port: 8000

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka

spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/user-service/checkAT
            - Path=/user-service/**
            - Method=GET,POST,PUT
#          filters:
#            - RemoveRequestHeader=Cookie
#            - RewritePath=/auth-service/(?<segment>.*),/$\{segment}
#            - AuthorizationFilter

        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/user-service/**
            - Method=GET,POST,PUT
#          filters:
#            - RemoveRequestHeader=Cookie
#            - RewritePath=/auth-service/(?<segment>.*),/$\{segment}
            #- AuthorizationFilter

        - id: festival-service
          uri: lb://FESTIVAL-SERVICE
          predicates:
            - Path=/festival-service/**
            - Method=GET,POST
#          filters:
#            - RemoveRequestHeader=Cookie
#            - RewritePath=/auth-service/(?<segment>.*),/$\{segment}

        - id: recomm-service
          uri: lb://RECOMM-SERVICE
          predicates:
            - Path=/recomm-service/**
            - Method=GET,POST
#          filters:
#            - RemoveRequestHeader=Cookie
#            - RewritePath=/auth-service/(?<segment>.*),/$\{segment}

        - id: auth-service
          uri: lb://AUTH-SERVICE
          predicates:
            - Path=/auth-service/**
            - Method=GET,POST,PUT,DELETE
          filters:
#            - RemoveRequestHeader=Cookie
#            - RewritePath=/auth-service/(?<segment>.*),/$\{segment}
             #- AuthorizationFilter
            # - JwtAuthenticationFilter
  # bean중복 에러 해결
  main:
    allow-bean-definition-overriding: true
token:
  secret: secretKey
  refresh: refreshKey